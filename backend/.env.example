# Server Configuration
PORT=8007
HOST=0.0.0.0

# CORS Configuration
CORS_ORIGINS=http://localhost:3007,http://127.0.0.1:3007
## Your CORS origins can be a comma-separated list of URLs

# Model Configuration
# DEVICE options: auto (recommended), cuda, mps, cpu
# auto = automatically detect best available device
DEVICE=auto
DEFAULT_MODEL=hyper
CONFIG_PATH=configs/fastface/am1_and_dcg.json

# File Storage
UPLOAD_DIR=uploads
OUTPUT_DIR=output
MAX_FILE_SIZE=10485760

# Gemini API for VLM-based prompt generation
GEMINI_API_KEY=your_gemini_api_key_here
